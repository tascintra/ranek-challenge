import styled from "styled-components";

const Title = styled.h2`
  font-size: 1.25rem;
  font-weight: bold;
  font-family: serif;
  
  @media (min-width: 364px) {
    
    font-size: 1.6rem;
  }
`;

export default Title;